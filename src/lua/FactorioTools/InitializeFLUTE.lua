-- Generated by CSharp.lua Compiler
local System = System
local KnapcodeFluteSharp = Knapcode.FluteSharp
System.namespace("Knapcode.FactorioTools.OilField", function (namespace)
  namespace.class("InitializeFLUTE", function (namespace)
    local FLUTELock, Execute, class, static
    static = function (this)
      FLUTELock = System.Object()
    end
    Execute = function (lutD)
      if lutD ~= 6 then
        System.throw(System.ArgumentOutOfRangeException("lutD" --[[nameof(lutD)]], "Only lookup table of degree 6 is supported."))
      end

      -- lock(FLUTELock)
      do
        if class.FLUTE ~= nil then
          return
        end

        local lookUpTable = KnapcodeFluteSharp.LookUpTable.getDegree6()
        local flute = KnapcodeFluteSharp.FLUTE(lookUpTable)
        class.FLUTE = flute
      end
    end
    class = {
      Execute = Execute,
      static = static
    }
    return class
  end)
end)
