-- Generated by CSharp.lua Compiler
local System = System
System.namespace("Knapcode.FactorioTools.OilField", function (namespace)
  namespace.class("ListTableArray_1", function (namespace)
    return function (T)
      local get, set, getCount, Add, AddCollection, AddRange, Clear, Contains, 
      EnumerateItems, Remove, RemoveAt, RemoveRange, Reverse, Sort, SortRange, class, 
      __ctor1__, __ctor2__
      local ListT = System.List(T)
      __ctor1__ = function (this)
        this._list = ListT()
      end
      __ctor2__ = function (this, capacity)
        this._list = ListT(capacity)
      end
      get = function (this, index)
        return this._list:get(index)
      end
      set = function (this, index, value)
        this._list:set(index, value)
      end
      getCount = function (this)
        return #this._list
      end
      Add = function (this, item)
        this._list:Add(item)
      end
      AddCollection = function (this, collection)
        this._list:AddRange(collection)
      end
      AddRange = function (this, collection)
        this._list:AddRange((System.cast(class, collection))._list)
      end
      Clear = function (this)
        this._list:Clear()
      end
      Contains = function (this, item)
        return this._list:Contains(item)
      end
      EnumerateItems = function (this)
        return this._list
      end
      Remove = function (this, item)
        return this._list:Remove(item)
      end
      RemoveAt = function (this, index)
        this._list:RemoveAt(index)
      end
      RemoveRange = function (this, index, count)
        this._list:RemoveRange(index, count)
      end
      Reverse = function (this)
        this._list:Reverse()
      end
      Sort = function (this, comparison)
        this._list:Sort(comparison)
      end
      SortRange = function (this, index, count, comparer)
        this._list:Sort(index, count, comparer)
      end
      class = {
        base = function (out)
          return {
            out.Knapcode.FactorioTools.OilField.ITableArray_1(T)
          }
        end,
        get = get,
        set = set,
        getCount = getCount,
        Add = Add,
        AddCollection = AddCollection,
        AddRange = AddRange,
        Clear = Clear,
        Contains = Contains,
        EnumerateItems = EnumerateItems,
        Remove = Remove,
        RemoveAt = RemoveAt,
        RemoveRange = RemoveRange,
        Reverse = Reverse,
        Sort = Sort,
        SortRange = SortRange,
        __ctor__ = {
          __ctor1__,
          __ctor2__
        }
      }
      return class
    end
  end)
end)
